
public class STRING_exercise {
    public static void main(String[] args)
    {
     //   int num1 = 12;
     //   String name = "ROOM";

     //   int length = name.length(); //counts the number of indices in 'name'

      //  char letter = name.charAt(1);  //Tells the character at the specified index

      //  int index = name.indexOf("o");  //tells the first index of the character

      //  int index_last = name.lastIndexOf("o"); //tells the last most appeared index of the character

       // name = name.toLowerCase();  //converts all characters to lower case

       // name = name.toUpperCase();  //converts all characters to upper case


      //  System.out.println(name);


    }
}
